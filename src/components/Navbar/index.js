import React from "react";
import { Nav, NavSet } from "./styled";

const Navbar = () => {
  return (
    <Nav>
      <NavSet to="/">
          LILITAN
      </NavSet>
    </Nav>
  );
};

export default Navbar;
